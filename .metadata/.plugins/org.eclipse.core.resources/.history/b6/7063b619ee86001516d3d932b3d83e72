class World:
    def __init__(self, dim):
        self.worldObjects = []
        return
    
    def addWorldObject(self, worldObject):
        assert worldObject.dim == self.dim, "Incorrect dimension for "
        self.worldObjects.append(worldObject)
        