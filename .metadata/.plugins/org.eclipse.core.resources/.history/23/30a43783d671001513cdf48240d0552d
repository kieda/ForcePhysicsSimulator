package edu.cmu.cs.graphics.crowdsim.ai.module;

/**
 * Tells the module runtime system which module types to accept.
 * 
 * Suppose we want to listen on some classes A through B where A instanceof B
 * and maybe A == B.
 * 
 * We specify A using value(), and B using min().
 * 
 * Example code that listens to 
 * 
 * @author zkieda
 */
public @interface ListenOn {
	public Class<? extends SubModule<?>> value();
}
